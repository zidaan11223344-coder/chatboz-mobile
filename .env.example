# Server/database values required by the existing backend
DATABASE_URL=mysql://user:password@host:3306/database
JWT_SECRET=replace-with-a-long-random-secret

# The admin account is provisioned in the existing database/environment.
# Do not add or replace LOCAL_ADMIN_USERNAME/LOCAL_ADMIN_PASSWORD here.

# Optional platform/service values used by the existing server
OAUTH_SERVER_URL=
OWNER_OPEN_ID=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
