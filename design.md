# تصميم واجهات «شات باز» للجوال

## التوجه العام

«شات باز» تطبيق اجتماعي عربي يركّز على اكتشاف الغرف الصوتية المباشرة، متابعة المحادثات الخاصة، والتفاعل عبر النقاط والهدايا. تُبنى الواجهة للوضع الرأسي 9:16 مع دعم RTL كامل، وتبقى إجراءات الدخول إلى الغرفة ورفع اليد وكتم الميكروفون والرسالة ضمن متناول الإبهام في الجزء السفلي من الشاشة. تعتمد التجربة على الأنماط الأصلية المألوفة في iOS: تنقّل سفلي ثابت، رؤوس شاشات خفيفة، وبطاقات ذات استجابة لمس واضحة.

## قائمة الشاشات

| الشاشة | المحتوى والوظائف |
|---|---|
| الغرف | رأس العلامة والنقاط والتنبيهات، بطاقة اكتشاف لغرفة حيّة، بحث، تصنيفات، وبطاقات الغرف مع عدد المستمعين والمتحدثين. |
| تفاصيل الغرفة | عنوان وموضوع الغرفة، منصة المتحدثين، الجمهور، رسالة سريعة، منتقي الهدايا، ولوحة تحكم سفلية للهدية ورفع اليد وكتم الصوت والمغادرة. |
| الرسائل | بحث في المحادثات، قائمة المحادثات، حالة الاتصال، وآخر رسالة وعدد الرسائل غير المقروءة. |
| المحادثة الخاصة | رأس المستلم، فقاعات الرسائل، حقل كتابة متعدد الأسطر، زر إرسال، ومنتقي هدايا. |
| الملف الشخصي | بطاقة المستخدم، النقاط، ملخص النشاط، روابط الإعدادات، وحالة الاتصال عند توفرها. |
| الإعدادات وتسجيل الدخول والإدارة | إعداد عنوان الخادم وحالته، تسجيل الدخول إلى الخادم، وخيارات الإدارة بحسب صلاحية المستخدم؛ تبقى هذه المسارات امتدادًا للوظائف المرجعية ولا تُعيد تصميم طبقة البيانات. |

## التدفقات الرئيسية

| المهمة | مسار الاستخدام |
|---|---|
| اكتشاف غرفة | يفتح المستخدم تبويب الغرف ← يبحث أو يختار تصنيفًا ← يضغط بطاقة غرفة ← يصل إلى شاشة الغرفة الحيّة. |
| المشاركة في غرفة | يفتح المستخدم الغرفة ← يرى منصة المتحدثين ← يرفع يده أو يكتم الميكروفون ← يرسل هدية أو رسالة سريعة ← يغادر عبر لوحة التحكم السفلية. |
| مراسلة عضو | يفتح المستخدم تبويب الرسائل ← يختار محادثة ← يكتب رسالة أو يختار هدية ← تظهر الرسالة في سياق المحادثة وتُحدّث معاينة القائمة. |
| ضبط الاتصال | يفتح المستخدم الملف الشخصي ← الإعدادات ← يراجع عنوان الخادم وحالته ← يحفظ الإعدادات محليًا دون أي تعديل على قاعدة البيانات. |

## الألوان والهوية

| العنصر | اللون | الاستخدام |
|---|---|---|
| بنفسجي العلامة | `#5B5CE2` | الأزرار الأساسية، الحالة النشطة، وعلامة التطبيق. |
| حبر داكن | `#252536` | العناوين والنص الرئيسي. |
| رمادي هادئ | `#777789` | النص الثانوي والطوابع الزمنية. |
| خلفية لؤلؤية | `#F8F8FC` | خلفيات الشاشات والبطاقات البيضاء. |
| أخضر البث | `#18A873` | حالة الغرف المباشرة والنجاح. |
| مرجاني تفاعلي | `#FF7A59` | اللمسات الثانوية والهدايا. |

## قواعد التفاعل والتنفيذ

- تستخدم القوائم الطويلة `FlatList` لضمان أداء سلس، ويستخدم كل زر حالة ضغط مرئية ورد فعل لمسي خفيف عند توفره.
- تبقى لوحة تحكم الغرفة وحقل الرسالة ضمن المساحات الآمنة، ولا تتداخل مع منطقة المنزل أو لوحة المفاتيح.
- تظهر الحالات المؤقتة مثل الانضمام والإرسال وفشل الاتصال بوضوح، وتُعطل الإجراءات التي لا يمكن تنفيذها بدلاً من تركها بلا استجابة.
- تظل طبقة البيانات كما هي: التطبيق يقرأ إعدادات الخادم محليًا ويتصل بالمسارات المرجعية فقط عند تفاعل المستخدم؛ لا تتضمن واجهة الجوال أي ترحيل أو تعديل للمخطط أو لبيانات قاعدة البيانات.
